# XAL-715: [SEO ROI 48] Løft rangering: leie bryllupslokale

> Auto-prepared by Digilist Improvements Agent. Run Claude in this worktree:
> `/loop In the marketing repo at /root/booking-brilliance, fix a technical-SEO gap affecting the keyword 'leie bryllupslokale' (currently ranks position 14, target top-10). Investigation confirmed the content already exists — src/content/blog/leie-bryllupslokale.md targets this exact keyword in its frontmatter, and src/pages/LeieSelskapslokale.tsx (route /leie/selskapslokale) covers 'bryllup' as a sub-intent and links to the post — so do NOT create a new page. The real gap: public/sitemap.xml only lists 12 <url> entries while the repo has 97 blog posts (src/content/blog/*.md) and 13 /leie/* category routes (see src/App.tsx lines ~307-332), so almost nothing is discoverable via the sitemap, including this exact page. Steps: (1) Add missing <url> entries to public/sitemap.xml for at least /blogg/leie-bryllupslokale and the closely related bryllup posts (bryllupslokale-forening-utleie-og-booking-festsal, bryllupslokale-kommune-godkjenning-vigsel-skjenkebevilling-catering-depositum, bryllupslokale-kommune-pris-privat-sammenligning, bryllupslokale-kommune-sjekkliste-bryllupsdagen, leie-selskapslokale-bryllup-fest) and /leie/selskapslokale, matching the existing <loc>/<lastmod>/<changefreq>/<priority> format and using a real lastmod date. (2) Check whether the sitemap is manually maintained or should be generated by a build script — if there's no generator, just hand-add the entries needed for this ticket rather than boiling the ocean on all 97 posts, but flag the broader staleness in the PR description. (3) Verify src/pages/LeieSelskapslokale.tsx already links to the leie-bryllupslokale blog post with descriptive anchor text containing 'bryllupslokale'; if the anchor text is generic ('les mer'), tighten it to include the keyword. (4) Do not touch unrelated routes or content. Acceptance criteria: sitemap.xml is valid XML, includes the leie-bryllupslokale blog URL and the listed related URLs, and the internal link from /leie/selskapslokale uses keyword-relevant anchor text. Run any existing lint/build checks and confirm they pass green before opening a PR. Note in the PR description that actual SERP position (position ≤10 goal) depends on factors outside this repo (backlinks, competition) and cannot be verified from code alone — recommend the requester check GSC/SERP tracking after deploy per the ticket's 'How to verify' section.`

## Implementation contract — complete this before writing code
- **Problem:** In the marketing repo at /root/booking-brilliance, fix a technical-SEO gap affecting the keyword 'leie bryllupslokale' (currently ranks position 14, target top-10). Investigation confirmed the content already exists — src/content/blog/leie-bryllupslokale.md targets this exact keyword in its frontmatter, and src/pages/LeieSelskapslokale.tsx (route /leie/selskapslokale) covers 'bryllup' as a sub-intent and links to the post — so do NOT create a new page. The real gap: public/sitemap.xml only lists 12 <url> entries while the repo has 97 blog posts (src/content/blog/*.md) and 13 /leie/* category routes (see src/App.tsx lines ~307-332), so almost nothing is discoverable via the sitemap, including this exact page. Steps: (1) Add missing <url> entries to public/sitemap.xml for at least /blogg/leie-bryllupslokale and the closely related bryllup posts (bryllupslokale-forening-utleie-og-booking-festsal, bryllupslokale-kommune-godkjenning-vigsel-skjenkebevilling-catering-depositum, bryllupslokale-kommune-pris-privat-sammenligning, bryllupslokale-kommune-sjekkliste-bryllupsdagen, leie-selskapslokale-bryllup-fest) and /leie/selskapslokale, matching the existing <loc>/<lastmod>/<changefreq>/<priority> format and using a real lastmod date. (2) Check whether the sitemap is manually maintained or should be generated by a build script — if there's no generator, just hand-add the entries needed for this ticket rather than boiling the ocean on all 97 posts, but flag the broader staleness in the PR description. (3) Verify src/pages/LeieSelskapslokale.tsx already links to the leie-bryllupslokale blog post with descriptive anchor text containing 'bryllupslokale'; if the anchor text is generic ('les mer'), tighten it to include the keyword. (4) Do not touch unrelated routes or content. Acceptance criteria: sitemap.xml is valid XML, includes the leie-bryllupslokale blog URL and the listed related URLs, and the internal link from /leie/selskapslokale uses keyword-relevant anchor text. Run any existing lint/build checks and confirm they pass green before opening a PR. Note in the PR description that actual SERP position (position ≤10 goal) depends on factors outside this repo (backlinks, competition) and cannot be verified from code alone — recommend the requester check GSC/SERP tracking after deploy per the ticket's 'How to verify' section.
- **Business objective:** _why this matters (from the Linear issue)_
- **Repository / branch:** `/root/booking-brilliance` @ `agent/xal-715-loft-rangering-leie-bryllupslokale`
- **Scope:** _the one change this branch delivers_
- **Out of scope:** _what you will NOT touch — no opportunistic refactor, no formatting sweeps_
- **Acceptance criteria:** _observable, demonstrable outcomes_
- **Architecture constraints:** _boundaries + patterns to follow_
- **Files likely affected:** _list them; if this grows well beyond the list, escalate_
- **Testing requirements:** _what proves it works_
- **Security considerations:** _secrets, RBAC, injection, dependencies_
- **Rollback strategy:** _how to revert safely_
- **Definition of done:** compiled · tests green · acceptance demonstrated with evidence · one reviewable change · no attribution

## Delivery rules
- One issue → one branch (`agent/xal-715-loft-rangering-leie-bryllupslokale`) → one independently reviewable change. Never main.
- Smallest valid change. No opportunistic refactoring, no broad formatting changes, no hidden dependency on another open PR.
- Validation is mandatory and staged — "code written" ≠ "compiled" ≠ "tests passed" ≠ "acceptance demonstrated". Collect evidence (test output / logs) before opening the PR.
- If scope expands beyond "Files likely affected", or the change grows large, STOP and escalate ("BLOCKED:") rather than pressing on.
- Open a PR only when green (otherwise a draft PR with a note). Delete this file before opening the PR.

## Full issue — from Linear (the source of truth for scope & acceptance)
> The Linear MCP in this environment may be bound to the WRONG workspace, so
> do NOT rely on it to read this ticket — everything you need is below. If
> something essential is genuinely missing here (e.g. a screenshot), STOP and
> end with "CLARIFICATION:" rather than guessing.

**Classification:** improvement · severity minor · priority P3

Product gap: [SEO ROI 48] Løft rangering: leie bryllupslokale. <!-- xaheen-triage -->
**enhancement** · value low — Opportunity Agent's own factors show low revenue potential (33/100) despite commercial intent; ROI 48/100 is a middling score, not a named commitment or blocked user — treat as a modest, data-scored opportunity, not a high-value ask.

The Opportunity Agent scored the keyword/page "leie bryllupslokale" at ROI 48/100 (value-ranked, not by search volume). It currently ranks position 14 in search results; the report frames this as an opportunity to push it into the top 10.

**Done when**

- [ ] The tracked page/keyword "leie bryllupslokale" reaches a top-10 SERP position (currently pos 14)

**How to verify**

* Check GSC/SERP tracking for "leie bryllupslokale" after changes go live and confirm position ≤10

**Open questions**

* Which URL/page is being targeted for "leie bryllupslokale"? The report gives a keyword and position but no page link.
* What concrete action closes this — content rewrite, internal linking, technical SEO, backlinks? The report only gives scores, not a prescribed fix.
* Is this page on the marketing site or inside the product (a listing/category page)? Determines which repo actually owns it.

<details><summary>Reporter's original text</summary>

**ROI-score 48/100** — rangert av Opportunity Agent på verdi, ikke søkevolum.

| Faktor | Verdi |
| -- | -- |
| Inntektspotensial | 33 |
| Innsats | 45 |
| Konkurranse | 40 |
| Datasikkerhet | 65 |
| Intensjon | commercial |

**Hvorfor:** rangerer pos 14 — løft til topp-10

Kilde: digital tvilling (GSC + SERP + page_intel). Type: `improve-ranking`.

</details> Current assessment: partial (improvement, minor). Relevant code: src/content/blog/leie-bryllupslokale.md, src/pages/LeieSelskapslokale.tsx, public/sitemap.xml, src/App.tsx:307-332.

**Scope**
No new page needs to be built — a page and a keyword-matched blog post already cover 'leie bryllupslokale'. The concrete, verifiable technical-SEO fix is: add the missing URLs to public/sitemap.xml (at minimum /blogg/leie-bryllupslokale and the related bryllup posts: bryllupslokale-forening-utleie-og-booking-festsal, bryllupslokale-kommune-godkjenning-vigsel-skjenkebevilling-catering-depositum, bryllupslokale-kommune-pris-privat-sammenligning, bryllupslokale-kommune-sjekkliste-bryllupsdagen, leie-selskapslokale-bryllup-fest, plus /leie/selskapslokale), following the existing <url>/<loc>/<lastmod>/<changefreq>/<priority> format. Also verify /leie/selskapslokale and the blog post interlink with keyword-rich anchor text ('leie bryllupslokale') since that page is the closest topical match and can pass authority to the blog post. Do not fabricate a ranking guarantee — sitemap/interlink fixes are a legitimate lever but position ≤10 depends on factors outside this repo (backlinks, SERP competition) that cannot be verified here. Touch points: src/content/blog/leie-bryllupslokale.md (blog post already exists with frontmatter keywords ['leie bryllupslokale', 'bryllupslokale pris', ...] — exact-match content already targets this keyword); src/pages/LeieSelskapslokale.tsx (route /leie/selskapslokale already targets 'bryllup' as one of several sub-intents (selskapslokale, jubileum, konfirmasjon, fest) and links to the blog post); public/sitemap.xml (only 12 <url> entries total vs 97 blog posts + 13 /leie/* category routes in the app — leie-bryllupslokale and nearly all other blog/category URLs are missing from the sitemap, a plain technical-SEO gap that limits crawl/index priority for the exact page this ticket targets); src/App.tsx:307-332 (full list of /leie/* routes, none named bryllupslokale specifically — closest is /leie/selskapslokale).

**Done when**

- [ ] No new page needs to be built — a page and a keyword-matched blog post already cover 'leie bryllupslokale'. The concrete, verifiable technical-SEO fix is: add the missing URLs to public/sitemap.xml (at minimum /blogg/leie-bryllupslokale and the related bryllup posts: bryllupslokale-forening-utleie-og-booking-festsal, bryllupslokale-kommune-godkjenning-vigsel-skjenkebevilling-catering-depositum, bryllupslokale-kommune-pris-privat-sammenligning, bryllupslokale-kommune-sjekkliste-bryllupsdagen, leie-selskapslokale-bryllup-fest, plus /leie/selskapslokale), following the existing <url>/<loc>/<lastmod>/<changefreq>/<priority> format. Also verify /leie/selskapslokale and the blog post interlink with keyword-rich anchor text ('leie bryllupslokale') since that page is the closest topical match and can pass authority to the blog post. Do not fabricate a ranking guarantee — sitemap/interlink fixes are a legitimate lever but position ≤10 depends on factors outside this repo (backlinks, SERP competition) that cannot be verified here.

## Code analysis (evidence, marketing @ d5325012)

Status: **partial** (confidence 85%)

* `src/content/blog/leie-bryllupslokale.md` — blog post already exists with frontmatter keywords ['leie bryllupslokale', 'bryllupslokale pris', ...] — exact-match content already targets this keyword
* `src/pages/LeieSelskapslokale.tsx` — route /leie/selskapslokale already targets 'bryllup' as one of several sub-intents (selskapslokale, jubileum, konfirmasjon, fest) and links to the blog post
* `public/sitemap.xml` — only 12 <url> entries total vs 97 blog posts + 13 /leie/* category routes in the app — leie-bryllupslokale and nearly all other blog/category URLs are missing from the sitemap, a plain technical-SEO gap that limits crawl/index priority for the exact page this ticket targets
* `src/App.tsx:307-332` — full list of /leie/* routes, none named bryllupslokale specifically — closest is /leie/selskapslokale

## Source

Product idea (XAL-715), from search demand

## Run as Claude loop (in `/root/booking-brilliance`, on a new branch)

```
/loop In the marketing repo at /root/booking-brilliance, fix a technical-SEO gap affecting the keyword 'leie bryl

Linear: https://linear.app/xala-technologies/issue/XAL-715/seo-roi-48-loft-rangering-leie-bryllupslokale
