# XAL-327: Self-learning & improvement layer — capture / distill / inject (Phase 1)

> Auto-forberedt av Digilist Improvements Agent. Kjør Claude i denne worktreen:
> `/loop Build the self-learning & improvement layer for the Digilist agent fleet — a cross-agent knowledge system that learns from SIX sources: (1) repository patterns, (2) industry best practices, (3) its own mistakes, (4) user feedback, (5) content signals (blogs/keywords), and (6) LATEST TECHNOLOGY TRENDS + stack documentation (continuously stay current per stack). It feeds those learnings back into EVERY agent so the fleet compounds over time and always evolves with the ecosystem. GENERALIZES the OB1-style content-memory and the Open Brain learnings[] into one fleet-wide layer. REUSE (do not duplicate): OpenBrain (tools/improvements-agent/src/brain.ts), runClaudeAgent (tools/content-agent/src/claude-agent.ts) + CAPABILITY_PREAMBLE, codebase-memory (repo patterns), docs-rag, content memory. New module: tools/knowledge-agent/. The loop is capture -> distill -> inject.

STORE = HYBRID (answers the "wiki vs Open Brain" question — use both):
- Open Brain is the BACKBONE machine store (structured, provenance/confidence/dedup, what agents write+read programmatically). Extend it; do NOT create a competing store.
- ALSO materialize distilled learnings as a human-readable WIKI: a KNOWLEDGE.md index + docs/knowledge/<topic>.md files (like the memory system), so a human can browse+curate them AND docs-rag can index them for semantic recall. One source of truth (Open Brain) rendered to the wiki on each distill.

PHASE 1:
1. Knowledge store (src/knowledge.ts): structured provenance-tracked store (extends the Open Brain JSON, gitignored). Learning = { id, type: 'repo-pattern'|'best-practice'|'mistake'|'user-feedback'|'content-signal'|'tech-trend', statement, why, applies_to (agent|domain|path-glob), source_ref, confidence 0..1, created_at, hits, last_applied }. recall(query|agent|type) ranks by relevance (applies_to + keyword + confidence + recency). Dedup by normalized statement. Render to KNOWLEDGE.md + docs/knowledge/*.md.
2. Capture (src/capture.ts): helpers to record raw signals — request-changes/rejected PR reviews, CI failures an agent fixed, false-positive/false-'gap' verdicts, blocked implement runs (BLOKKERT/AVKLARING), captureUserFeedback(text) for user corrections, and content signals from the content memory. Wire the OBVIOUS capture points in existing agents (pr-review verdict, implement blocked/no-PR, analyze verdict) with minimal safe additions.
3. Distillation (src/distill.ts -> "learning:run"): a capable Opus agent that reviews raw signals + mines REPO PATTERNS via codebase-memory + pulls INDUSTRY BEST PRACTICES and LATEST STACK DOCS/TRENDS via WebSearch/docs (TypeScript/Convex/React/Vite/WCAG/security — detect new major versions, deprecations, best-practice shifts) -> distills concrete, provenance-tracked, deduped learnings. When a trend implies an upgrade (e.g. a new major like TS7), AUTO-FILE an improvement issue via the LinearClient (same /loop format), so the fleet proposes its own evolution. Demote/remove learnings that stopped applying.
4. Injection (src/inject.ts): relevantLearnings(agent, context) -> compact token-budgeted top-N block; extend CAPABILITY_PREAMBLE so agents APPLY learnings automatically. Closes the loop: mistake -> learning -> injected -> not repeated.
5. Runner + schedule + docs + tests: tools/knowledge-agent/vps-knowledge-runner.sh (mirror the others), package.json "learning:run", suggest a daily/weekly timer in README (don't install), .gitignore the store, tests for store+recall+dedup+wiki-render (no live network).

Constraints: unify with existing brain/memory/runner/codebase-memory — no duplicate stores. Learnings actionable + provenance-tracked, never fabricated. Injection compact. Auto-filed upgrade issues must be advisory (Backlog, behind the human Todo gate). Norwegian bokmaal in user-facing text, no em-dash. typecheck + lint + build green before PR. If blocked, end with "BLOKKERT:".`

## Mål
Build the self-learning & improvement layer for the Digilist agent fleet — a cross-agent knowledge system that learns from SIX sources: (1) repository patterns, (2) industry best practices, (3) its own mistakes, (4) user feedback, (5) content signals (blogs/keywords), and (6) LATEST TECHNOLOGY TRENDS + stack documentation (continuously stay current per stack). It feeds those learnings back into EVERY agent so the fleet compounds over time and always evolves with the ecosystem. GENERALIZES the OB1-style content-memory and the Open Brain learnings[] into one fleet-wide layer. REUSE (do not duplicate): OpenBrain (tools/improvements-agent/src/brain.ts), runClaudeAgent (tools/content-agent/src/claude-agent.ts) + CAPABILITY_PREAMBLE, codebase-memory (repo patterns), docs-rag, content memory. New module: tools/knowledge-agent/. The loop is capture -> distill -> inject.

STORE = HYBRID (answers the "wiki vs Open Brain" question — use both):
- Open Brain is the BACKBONE machine store (structured, provenance/confidence/dedup, what agents write+read programmatically). Extend it; do NOT create a competing store.
- ALSO materialize distilled learnings as a human-readable WIKI: a KNOWLEDGE.md index + docs/knowledge/<topic>.md files (like the memory system), so a human can browse+curate them AND docs-rag can index them for semantic recall. One source of truth (Open Brain) rendered to the wiki on each distill.

PHASE 1:
1. Knowledge store (src/knowledge.ts): structured provenance-tracked store (extends the Open Brain JSON, gitignored). Learning = { id, type: 'repo-pattern'|'best-practice'|'mistake'|'user-feedback'|'content-signal'|'tech-trend', statement, why, applies_to (agent|domain|path-glob), source_ref, confidence 0..1, created_at, hits, last_applied }. recall(query|agent|type) ranks by relevance (applies_to + keyword + confidence + recency). Dedup by normalized statement. Render to KNOWLEDGE.md + docs/knowledge/*.md.
2. Capture (src/capture.ts): helpers to record raw signals — request-changes/rejected PR reviews, CI failures an agent fixed, false-positive/false-'gap' verdicts, blocked implement runs (BLOKKERT/AVKLARING), captureUserFeedback(text) for user corrections, and content signals from the content memory. Wire the OBVIOUS capture points in existing agents (pr-review verdict, implement blocked/no-PR, analyze verdict) with minimal safe additions.
3. Distillation (src/distill.ts -> "learning:run"): a capable Opus agent that reviews raw signals + mines REPO PATTERNS via codebase-memory + pulls INDUSTRY BEST PRACTICES and LATEST STACK DOCS/TRENDS via WebSearch/docs (TypeScript/Convex/React/Vite/WCAG/security — detect new major versions, deprecations, best-practice shifts) -> distills concrete, provenance-tracked, deduped learnings. When a trend implies an upgrade (e.g. a new major like TS7), AUTO-FILE an improvement issue via the LinearClient (same /loop format), so the fleet proposes its own evolution. Demote/remove learnings that stopped applying.
4. Injection (src/inject.ts): relevantLearnings(agent, context) -> compact token-budgeted top-N block; extend CAPABILITY_PREAMBLE so agents APPLY learnings automatically. Closes the loop: mistake -> learning -> injected -> not repeated.
5. Runner + schedule + docs + tests: tools/knowledge-agent/vps-knowledge-runner.sh (mirror the others), package.json "learning:run", suggest a daily/weekly timer in README (don't install), .gitignore the store, tests for store+recall+dedup+wiki-render (no live network).

Constraints: unify with existing brain/memory/runner/codebase-memory — no duplicate stores. Learnings actionable + provenance-tracked, never fabricated. Injection compact. Auto-filed upgrade issues must be advisory (Backlog, behind the human Todo gate). Norwegian bokmaal in user-facing text, no em-dash. typecheck + lint + build green before PR. If blocked, end with "BLOKKERT:".

## Regler
- Jobb kun på denne branchen (`agent/xal-327-self-learning-improvement-layer-capture-distill-`), aldri main.
- Kjør bygg + tester. Åpne PR bare når de er grønne (ellers draft-PR med notat).
- Slett denne filen før du åpner PR.

Linear: https://linear.app/xala-technologies/issue/XAL-327/self-learning-and-improvement-layer-capture-distill-inject-phase-1
